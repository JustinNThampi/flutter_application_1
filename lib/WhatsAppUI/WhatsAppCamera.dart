import 'package:flutter/material.dart';

class WhatsAppCamera extends StatelessWidget {
  const WhatsAppCamera({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Camera'),
      ),
    );
  }
}